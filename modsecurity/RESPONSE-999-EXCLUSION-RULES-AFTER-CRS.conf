# Exemplo de regra para permitir um IP específico sem restrições # Substitua os asteriscos pelo IP que você deseja permitir
#SecRule REMOTE_ADDR "@ipMatch 192.168.1.100" \ "id:1013,\ phase:1,\ nolog,\ allow,\ ctl:ruleEngine=Off" 
# Exemplo de regra para permitir um caminho específico sem restrições # 
#SecRule REQUEST_URI "@beginsWith /api/webhook" \ # "id:1014,\ # phase:1,\ # nolog,\ # allow,\ # ctl:ruleEngine=Off

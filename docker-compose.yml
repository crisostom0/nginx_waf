version: '3'

services:
  nginx-waf:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/share/nginx/html
    restart: unless-stopped
    networks:
      - web-network

networks:
  web-network:
    driver: bridge
